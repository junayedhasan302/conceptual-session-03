// 2. Given a string with leading and trailing spaces, print its length before trimming and after trimming to show the difference.
let str = "   Hello JavaScript   ";
console.log("Before Trim:", str.length);
let trimmed = str.trim();
console.log("After Trim:", trimmed.length);